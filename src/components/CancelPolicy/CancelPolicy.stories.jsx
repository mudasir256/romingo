import CancelPolicy from "./CancelPolicy";

export default {
  title: "CancelPolicy",
  component: CancelPolicy,
};

const Template = () => {
  return <CancelPolicy />;
};

export const Default = Template.bind({});
Default.args = {};
