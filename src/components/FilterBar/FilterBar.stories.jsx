import FilterBar from "./FilterBar";

export default {
  title: "FilterBar",
  component: FilterBar,
};

const Template = () => {
  return <FilterBar />;
};

export const Default = Template.bind({});
Default.args = {};
